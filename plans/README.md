# Implementation Plans

Generated by the improve skill on 2026-07-07, from a standard-depth audit of
the repo at commit `b4c094e92`. Execute in the order below unless dependencies
say otherwise. Each executor: read the plan fully before starting, honor its
STOP conditions, and update your row when done.

Scope note: the audit covered `packages/core`, `packages/tools`,
`packages/dicomImageLoader` source plus repo-wide test/CI/dependency posture.
`packages/core/src/RenderingEngine/GenericViewport/**` was deliberately
excluded (separately deep-reviewed in `GENERIC_VIEWPORT_ARCHITECTURE_REVIEW.md`
on branch cornerstoneBetterGenericViewport). `adapters`, `ai`,
`nifti-volume-loader`, `metadata` internals were not audited beyond test
coverage.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 010 | CI typecheck + api-check gate | P1 | S | — | TODO |
| 001 | Fix `cancelLoadAll` (`cancel` → `cancelFn`) | P1 | S | — | TODO |
| 002 | Fix `getVolumeContainingImageId` early return | P1 | S | — | TODO |
| 003 | Settle superseded StackViewport CPU load promise | P1 | S | — | TODO |
| 004 | Bind VideoViewport ELEMENT_DISABLED handler | P1 | S | — | TODO |
| 005 | Fix Synchronizer listener leaks (2 bugs) | P1 | M | — | TODO |
| 006 | Remove VolumeCroppingTool leaked global listener | P2 | S | — | TODO |
| 012 | Override `decompress` critical advisory | P2 | S | — | TODO |
| 007 | Geometry in cache budget + purge | P2 | M | 002 (same file) | TODO |
| 011 | Expand oxlint to `correctness` category | P2 | M | 010 (recommended) | TODO |
| 008 | Bound segmentation-stats worker payloads | P2 | M | — | TODO |
| 009 | Bound WholeBodySegmentTool island scan | P2 | M | 008 (soft — shared approach) | TODO |
| 013 | Scheduled Firefox/WebKit Playwright lane | P3 | M | — | TODO |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale)

## Dependency notes

- 010 first: it is the missing verification baseline; every other plan uses `pnpm run build:esm` as its typecheck gate.
- 007 after 002: both edit `packages/core/src/cache/cache.ts`; landing 002 first avoids conflicts.
- 011 after 010 (recommended): keeps lint failures separable from type failures during the ratchet.
- 009 references 008's bounded-extraction approach but can proceed independently.
- 001–006 are mutually independent; safe to run in parallel worktrees.

## Findings confirmed but not planned (available on request)

- OrientationControllerTool untracked `setTimeout`s outlive disable (`packages/tools/src/tools/OrientationControllerTool.ts:252-255,363-366,433-446`) — orphaned actors/listeners on fast enable/disable cycles.
- `extractMultipart` drops 3 trailing bytes on partial multipart reads (`packages/dicomImageLoader/src/imageLoader/wadors/extractMultipart.ts:58-83`, `slice(offset, endIndex - 2)` when `endIndex === -1`).
- Test-baseline group (declined this round): false-green `"test": "jest"` scripts in ai / polymorphic-segmentation / labelmap-interpolation (zero test files); no jest config for dicomImageLoader (119 src files).
- Annotation render loop invokes every enabled tool per frame (`packages/tools/src/stateManagement/annotation/AnnotationRenderingEngine.ts:189-212`).
- `getSegmentIndexAtWorldPoint` stack fallback returns raw label value, can return `undefined` despite `number` type (`packages/tools/src/utilities/segmentation/getSegmentIndexAtWorldPoint.ts:161-174`).
- `fillCircle` ellipse predicate divides by zero on degenerate brushes → silent no-op paint (`packages/tools/src/tools/segmentation/strategies/fillCircle.ts:321-393`).
- Unguarded `imagePlaneModule` deref in `createAndCacheDerivedImage` (`packages/core/src/loaders/imageLoader.ts:282-287,350-357`).
- Null guards missing in `removeEnabledElement._removeSvgNode` (`packages/tools/src/store/removeEnabledElement.ts:123-128`) and `VolumeCroppingTool.onCameraModified` (`:381-388`).
- README uses `yarn` commands in a pnpm-pinned repo (`README.md:206,214`); no root CLAUDE.md/AGENTS.md; `@types/node` 20 vs `engines: node >=24`; missing READMEs in 4 published packages; three bundlers (webpack+rspack+rollup 3); Karma+Jasmine EOL runner with puppeteer 13; flat 126-entry `core/utilities` barrel.

## Direction options (maintainer's call; design-spike plans on request)

- `@cornerstonejs/ai`: published but embryonic (7 files, one model, no tests) — define the model-adapter interface and add a second model behind it.
- `QuadraticSpline`: fully stubbed abstract class, nothing subclasses it — implement or delete.
- `TODO(next)` MPR/oblique-reformat cluster in `CrosshairsTool.ts:1680,2274`.

## Findings considered and rejected

- XSS via annotation labels: not a finding — all SVG text rendering goes through `textContent` (`drawTextBox.ts:96,196`); repo-wide grep found no HTML sinks in core/tools.
- Secrets / prompt-injection content: none found in audited scope.
- vtk.js / comlink / gl-matrix version drift across workspace: none — pinned consistently; vtk.js 35.x is current.
- Legacy viewport god classes & legacy↔GenericViewport duplication: by design — in-progress migration, separately reviewed.
- `requestPoolManager.sendRequests` counter leak: checked, correct (non-promise branch decrements).
- Playwright volume-test slowness: already planned in `VOLUME_TEST_SLICE_LIMIT_PLAN.md` (repo root) — decided work, not re-planned.
- `Synchronizer.fireEvent` async `_ignoreFiredEvents` gating: intentional loop prevention, not a race.
- drawTextBox stale `tspan`s when line count shrinks: cosmetic only (blanked to '').
- TypeScript 5.5 → 5.9 bump: no concrete cost surfaced; not worth standalone churn.
