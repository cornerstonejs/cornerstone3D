import { metaData } from '@cornerstonejs/metadata';

export const {
  addProvider,
  addTypedProvider,
  typedProviderProvider,
  get,
  removeProvider,
  removeAllProviders,
  getMetaData,
  clearQuery,
  getNormalized,
  toUpperCamelTag,
  toLowerCamelTag,
} = metaData;
