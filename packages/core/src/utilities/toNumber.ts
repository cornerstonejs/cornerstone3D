/** @deprecated Import from `@cornerstonejs/utils` instead. */
import {
  toNumber,
  toFiniteNumber,
  toNumber as toNumberDefault,
} from '@cornerstonejs/utils';

export { toNumber, toFiniteNumber };
export default toNumberDefault;
