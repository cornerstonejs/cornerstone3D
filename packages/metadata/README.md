# CS3D Metadata

The Metadata module provides transforms from various metadata formats into the
standard internal formats for metadata, without having direct dependencies on
any of the UI elements or parsing elements that the rest of CS3D has.
